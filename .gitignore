# Machine-local: toolchain symlink farm, baseline cache, scratch workdirs, raw runs.
/var/

# Generated: every page and series is re-derived from data/ on each run, so
# nothing here is worth committing or worth resolving a conflict over.
#/target/
/obj_dir/

# Bazel
/bazel-*
MODULE.bazel.lock

manifest.json

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
/uv.lock

# Per-test converter workdirs and logs; generated by `lhdtrack import seed`.
/tests/*/.seed/

.DS_Store
