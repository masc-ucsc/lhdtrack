// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// The Pyrope twin of br_fifo_flops_harness.sv: same LFSR, same fold, same seed, so all
// three simulators must agree on `checksum` or the test fails.
//
// This is GENERATED and assumes the DUT is a `mod` with these port names. A
// hand-written idiomatic Pyrope module may present a different interface;
// adjusting this file is part of promoting the test to status.pyrope =
// "idiomatic".
// `file.entity`: import("br_fifo_flops") names the FILE, and calling it fails with
// "call to undefined function". The module inside it is br_fifo_flops.br_fifo_flops.
const dut = import("br_fifo_flops.br_fifo_flops")

pub mod br_fifo_flops_harness(rst:u1) -> (checksum:u64@[0]) {
  reg lfsr:u64 = 2611923443488327891
  reg sum:u64  = 0

  checksum = sum

  mut nxt:u64 = lfsr
  wrap nxt = nxt ^ (nxt << 13)
  wrap nxt = nxt ^ (nxt >> 7)
  wrap nxt = nxt ^ (nxt << 17)

  const r = dut(clk=1, rst=rst, push_valid=lfsr#[0], push_data=lfsr#[1..=63], pop_ready=lfsr#[1])

  mut acc:u64 = sum
  wrap acc = ((acc << 1) | acc#[63]) ^ r.push_ready
  wrap acc = ((acc << 1) | acc#[63]) ^ r.pop_valid
  wrap acc = ((acc << 1) | acc#[63]) ^ r.pop_data
  wrap acc = ((acc << 1) | acc#[63]) ^ r.full
  wrap acc = ((acc << 1) | acc#[63]) ^ r.full_next
  wrap acc = ((acc << 1) | acc#[63]) ^ r.slots
  wrap acc = ((acc << 1) | acc#[63]) ^ r.slots_next
  wrap acc = ((acc << 1) | acc#[63]) ^ r.empty
  wrap acc = ((acc << 1) | acc#[63]) ^ r.empty_next
  wrap acc = ((acc << 1) | acc#[63]) ^ r.items
  wrap acc = ((acc << 1) | acc#[63]) ^ r.items_next

  if rst != 0 {
    lfsr = 2611923443488327891
    sum  = 0
  } else {
    lfsr = nxt
    sum  = acc
  }
}
