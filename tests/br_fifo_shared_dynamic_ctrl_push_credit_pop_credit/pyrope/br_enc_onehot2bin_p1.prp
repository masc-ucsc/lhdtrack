pub comb br_enc_onehot2bin_p1::[timecheck=false](clk:u1, rst:u1, `in`:u2) -> (out_valid:u1, out:u1) {
  out = 0ub?
  out = if ((`in` >> 1) & 1) != 0 { 1 } else { 0 }
  out_valid = unsigned(`in` != 0)
}
