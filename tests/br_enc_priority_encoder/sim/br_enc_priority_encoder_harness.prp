// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// The Pyrope twin of br_enc_priority_encoder_harness.sv: same LFSR, same fold, same seed, so all
// three simulators must agree on `checksum` or the test fails.
//
// This is GENERATED and assumes the DUT is a `mod` with these port names. A
// hand-written idiomatic Pyrope module may present a different interface;
// adjusting this file is part of promoting the test to status.pyrope =
// "idiomatic".
// `file.entity`: import("br_enc_priority_encoder") names the FILE, and calling it fails with
// "call to undefined function". The module inside it is br_enc_priority_encoder.br_enc_priority_encoder.
const dut = import("br_enc_priority_encoder.br_enc_priority_encoder")

pub mod br_enc_priority_encoder_harness(rst:u1) -> (checksum:u64@[0]) {
  reg lfsr:u64 = 2611923443488327891
  reg sum:u64  = 0

  checksum = sum

  mut nxt:u64 = lfsr
  wrap nxt = nxt ^ (nxt << 13)
  wrap nxt = nxt ^ (nxt >> 7)
  wrap nxt = nxt ^ (nxt << 17)

  const r = dut(clk=1, rst=rst, `in`=lfsr#[0..=15])

  mut acc:u64 = sum
  wrap acc = ((acc << 1) | acc#[63]) ^ r

  if rst != 0 {
    lfsr = 2611923443488327891
    sum  = 0
  } else {
    lfsr = nxt
    sum  = acc
  }
}
