// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// The Pyrope twin of br_amba_atb_funnel_harness.sv: same LFSR, same fold, same seed, so all
// three simulators must agree on `checksum` or the test fails.
//
// This is GENERATED and assumes the DUT is a `mod` with these port names. A
// hand-written idiomatic Pyrope module may present a different interface;
// adjusting this file is part of promoting the test to status.pyrope =
// "idiomatic".
// `file.entity`: import("br_amba_atb_funnel") names the FILE, and calling it fails with
// "call to undefined function". The module inside it is br_amba_atb_funnel.br_amba_atb_funnel.
const dut = import("br_amba_atb_funnel.br_amba_atb_funnel")

pub mod br_amba_atb_funnel_harness(rst:u1) -> (checksum:u64@[0]) {
  reg lfsr:u64 = 2611923443488327891
  reg sum:u64  = 0

  checksum = sum

  mut nxt:u64 = lfsr
  wrap nxt = nxt ^ (nxt << 13)
  wrap nxt = nxt ^ (nxt >> 7)
  wrap nxt = nxt ^ (nxt << 17)

  const r = dut(clk=1, rst=rst, src_atvalid=lfsr#[0..=1], src_atid=lfsr#[2..=15], src_atdata=lfsr#[16..=63], src_atbytes=lfsr#[16..=19], src_atuser=lfsr#[20..=21], dst_atready=lfsr#[22])

  mut acc:u64 = sum
  wrap acc = ((acc << 1) | acc#[63]) ^ r.src_atready
  wrap acc = ((acc << 1) | acc#[63]) ^ r.dst_atvalid
  wrap acc = ((acc << 1) | acc#[63]) ^ r.dst_atid
  wrap acc = ((acc << 1) | acc#[63]) ^ r.dst_atdata
  wrap acc = ((acc << 1) | acc#[63]) ^ r.dst_atbytes
  wrap acc = ((acc << 1) | acc#[63]) ^ r.dst_atuser

  if rst != 0 {
    lfsr = 2611923443488327891
    sum  = 0
  } else {
    lfsr = nxt
    sum  = acc
  }
}
