// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// The Pyrope twin of br_ram_flops_harness.sv: same LFSR, same fold, same seed, so all
// three simulators must agree on `checksum` or the test fails.
//
// This is GENERATED and assumes the DUT is a `mod` with these port names. A
// hand-written idiomatic Pyrope module may present a different interface;
// adjusting this file is part of promoting the test to status.pyrope =
// "idiomatic".
// `file.entity`: import("br_ram_flops") names the FILE, and calling it fails with
// "call to undefined function". The module inside it is br_ram_flops.br_ram_flops.
const dut = import("br_ram_flops.br_ram_flops")

pub mod br_ram_flops_harness(rst:u1) -> (checksum:u64@[0]) {
  reg lfsr:u64 = 2611923443488327891
  reg sum:u64  = 0

  checksum = sum

  mut nxt:u64 = lfsr
  wrap nxt = nxt ^ (nxt << 13)
  wrap nxt = nxt ^ (nxt >> 7)
  wrap nxt = nxt ^ (nxt << 17)

  const r = dut(wr_clk=0, rd_clk=0, wr_rst=rst, rd_rst=rst, wr_valid=lfsr#[0], wr_addr=lfsr#[1..=6], wr_data=lfsr#[7..=63], wr_word_en=lfsr#[7..=14], rd_addr_valid=lfsr#[15], rd_addr=lfsr#[16..=21])

  mut acc:u64 = sum
  wrap acc = ((acc << 1) | acc#[63]) ^ r.rd_data_valid
  wrap acc = ((acc << 1) | acc#[63]) ^ r.rd_data

  if rst != 0 {
    lfsr = 2611923443488327891
    sum  = 0
  } else {
    lfsr = nxt
    sum  = acc
  }
}
