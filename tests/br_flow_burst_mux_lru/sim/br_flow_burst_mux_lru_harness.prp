// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// The Pyrope twin of br_flow_burst_mux_lru_harness.sv: same LFSR, same fold, same seed, so all
// three simulators must agree on `checksum` or the test fails.
//
// This is GENERATED and assumes the DUT is a `mod` with these port names. A
// hand-written idiomatic Pyrope module may present a different interface;
// adjusting this file is part of promoting the test to status.pyrope =
// "idiomatic".
// `file.entity`: import("br_flow_burst_mux_lru") names the FILE, and calling it fails with
// "call to undefined function". The module inside it is br_flow_burst_mux_lru.br_flow_burst_mux_lru.
const dut = import("br_flow_burst_mux_lru.br_flow_burst_mux_lru")

pub mod br_flow_burst_mux_lru_harness(rst:u1) -> (checksum:u64@[0]) {
  reg lfsr:u64 = 2611923443488327891
  reg sum:u64  = 0

  checksum = sum

  mut nxt:u64 = lfsr
  wrap nxt = nxt ^ (nxt << 13)
  wrap nxt = nxt ^ (nxt >> 7)
  wrap nxt = nxt ^ (nxt << 17)

  const r = dut(clk=0, rst=rst, push_valid=lfsr#[0..=3], push_last=lfsr#[4..=7], push_data=lfsr#[8..=63], pop_ready=lfsr#[8])

  mut acc:u64 = sum
  wrap acc = ((acc << 1) | acc#[63]) ^ r.push_ready
  wrap acc = ((acc << 1) | acc#[63]) ^ r.pop_valid_unstable
  wrap acc = ((acc << 1) | acc#[63]) ^ r.pop_last_unstable
  wrap acc = ((acc << 1) | acc#[63]) ^ r.pop_data_unstable

  if rst != 0 {
    lfsr = 2611923443488327891
    sum  = 0
  } else {
    lfsr = nxt
    sum  = acc
  }
}
