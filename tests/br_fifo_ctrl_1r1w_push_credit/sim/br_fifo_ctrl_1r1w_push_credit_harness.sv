// Generated by tools/gen_testbench.py -- lhdtrack-generated. Simulation only.
//
// Wraps br_fifo_ctrl_1r1w_push_credit with an LFSR stimulus generator and a rotate-xor checksum, so
// every simulator compiles and runs the SAME state machine. `checksum` is the
// only thing the driver reads.
`default_nettype none

module br_fifo_ctrl_1r1w_push_credit_harness (
    input  wire        clk,
    input  wire        rst,
    output wire [63:0] checksum
);

  logic [63:0] lfsr, sum;
  logic [63:0] nxt, acc;
  logic [0:0] o_push_receiver_in_reset;
  logic [0:0] o_push_credit;
  logic [0:0] o_pop_valid;
  logic [31:0] o_pop_data;
  logic [0:0] o_full;
  logic [0:0] o_full_next;
  logic [5:0] o_slots;
  logic [5:0] o_slots_next;
  logic [0:0] o_empty;
  logic [0:0] o_empty_next;
  logic [5:0] o_items;
  logic [5:0] o_items_next;
  logic [5:0] o_credit_count_push;
  logic [5:0] o_credit_available_push;
  logic [0:0] o_ram_wr_valid;
  logic [4:0] o_ram_wr_addr;
  logic [31:0] o_ram_wr_data;
  logic [0:0] o_ram_rd_addr_valid;
  logic [4:0] o_ram_rd_addr;

  // xorshift64 -- cheap, full-period, and trivially identical in both languages.
  always_comb begin
    nxt = lfsr ^ (lfsr << 13);
    nxt = nxt  ^ (nxt  >> 7);
    nxt = nxt  ^ (nxt  << 17);
  end

  always_comb begin
      acc = sum;
      acc = {acc[62:0], acc[63]} ^ 64'(o_push_receiver_in_reset);
      acc = {acc[62:0], acc[63]} ^ 64'(o_push_credit);
      acc = {acc[62:0], acc[63]} ^ 64'(o_pop_valid);
      acc = {acc[62:0], acc[63]} ^ 64'(o_pop_data);
      acc = {acc[62:0], acc[63]} ^ 64'(o_full);
      acc = {acc[62:0], acc[63]} ^ 64'(o_full_next);
      acc = {acc[62:0], acc[63]} ^ 64'(o_slots);
      acc = {acc[62:0], acc[63]} ^ 64'(o_slots_next);
      acc = {acc[62:0], acc[63]} ^ 64'(o_empty);
      acc = {acc[62:0], acc[63]} ^ 64'(o_empty_next);
      acc = {acc[62:0], acc[63]} ^ 64'(o_items);
      acc = {acc[62:0], acc[63]} ^ 64'(o_items_next);
      acc = {acc[62:0], acc[63]} ^ 64'(o_credit_count_push);
      acc = {acc[62:0], acc[63]} ^ 64'(o_credit_available_push);
      acc = {acc[62:0], acc[63]} ^ 64'(o_ram_wr_valid);
      acc = {acc[62:0], acc[63]} ^ 64'(o_ram_wr_addr);
      acc = {acc[62:0], acc[63]} ^ 64'(o_ram_wr_data);
      acc = {acc[62:0], acc[63]} ^ 64'(o_ram_rd_addr_valid);
      acc = {acc[62:0], acc[63]} ^ 64'(o_ram_rd_addr);
  end

  br_fifo_ctrl_1r1w_push_credit dut (
    .clk(clk),
    .rst(rst),
    .push_sender_in_reset(rst),
    .push_receiver_in_reset(rst),
    .push_credit_stall(lfsr[0]),
    .push_valid(lfsr[1]),
    .push_data(lfsr[33:2]),
    .pop_ready(lfsr[34]),
    .credit_initial_push(lfsr[40:35]),
    .credit_withhold_push(lfsr[46:41]),
    .ram_rd_data_valid(lfsr[47]),
    .ram_rd_data({16'd0, lfsr[63:48]}),
    .push_receiver_in_reset(o_push_receiver_in_reset),
    .push_credit(o_push_credit),
    .pop_valid(o_pop_valid),
    .pop_data(o_pop_data),
    .full(o_full),
    .full_next(o_full_next),
    .slots(o_slots),
    .slots_next(o_slots_next),
    .empty(o_empty),
    .empty_next(o_empty_next),
    .items(o_items),
    .items_next(o_items_next),
    .credit_count_push(o_credit_count_push),
    .credit_available_push(o_credit_available_push),
    .ram_wr_valid(o_ram_wr_valid),
    .ram_wr_addr(o_ram_wr_addr),
    .ram_wr_data(o_ram_wr_data),
    .ram_rd_addr_valid(o_ram_rd_addr_valid),
    .ram_rd_addr(o_ram_rd_addr)
  );

  always_ff @(posedge clk) begin
    if (rst) begin
      lfsr <= 64'd2611923443488327891;
      sum  <= 64'd0;
    end else begin
      lfsr <= nxt;
      sum  <= acc;
    end
  end

  assign checksum = sum;

endmodule

`default_nettype wire
